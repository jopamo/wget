/* Bounded string duplication helper
 * src/xstrndup.h
 */

#ifndef WGET_XSTRNDUP_H
#define WGET_XSTRNDUP_H

#include <stddef.h>

char* xstrndup(const char* s, size_t n);

#endif /* WGET_XSTRNDUP_H */
